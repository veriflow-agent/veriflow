# nixpacks.toml
# Let Python provider handle pip/venv automatically
# Only add Node and append frontend build

providers = ["python"]

[phases.setup]
nixPkgs = ["...", "nodejs_20"]

[phases.build]
cmds = ["...", "cd frontend && npm install && npm run build"]